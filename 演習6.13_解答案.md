# 演習6.13. 「同じ強連結成分内」という関係が頂点集合を同値類に分割することを証明しなさい。

## 準備

* グラフ $G = (V, E)$
  - $V$ は頂点の集合。
  - $E$ は辺 $(a, b)$ の集合。
    - $a$ が始点、$b$ が終点。

* 写像 $outs ~ v = \{v' \mid (v, v') \in E\}$
  - これは、頂点 $v$ を、$v$ を始点とする辺の終点の集合に写す写像。 ($outs ~ v \subseteq V$)

* 写像 $ins ~ v = \{v' \mid (v', v) \in E\}$
  - これは、頂点 $v$ を、$v$ を終点とする辺の始点の集合に写す写像。 ($ins ~ v \subseteq V$)

* 写像 $outs^\ast ~ v \quad (v \in V)$
  - これは、以下の条件を満たす
    - $\forall v \in V, (v \in outs^\ast ~ v)$
      - この条件より $outs^\ast ~ v \neq \varnothing$
    - $\forall v, v' \in V, (v' \in outs^\ast ~ v \implies outs ~ v' \subseteq outs^\ast ~ v)$

    推移閉包であり、頂点 $v$ を始点とする辺を頂点 $v$ から0本以上たどって到達可能な頂点の集合に写す写像。 ($outs^\ast ~ v \subseteq V$)

* 写像 $ins^\ast ~ v \quad (v \in V)$
  - これは、以下の条件
    - $\forall v \in V, (v \in ins^\ast ~ v)$
      - この条件より $ins^\ast ~ v \neq \varnothing$
    - $\forall v, v' \in V, (v' \in ins^\ast ~ v \implies ins ~ v' \subseteq ins^\ast ~ v)$

    を満たす推移閉包であり、頂点 $v$ を終点とする辺を頂点 $v$ から0本以上さかのぼって到達可能な頂点の集合に写す写像。  ($ins^\ast ~ v \subseteq V$)

* 強連結成分 $scc ~ v \quad (v \in V)$
  - これは、以下の様に表される集合であり、
    - $outs^\ast ~ v \cap ins^\ast ~ v$

    $v$ を始点/終点とする辺を0本以上たどって$v$と互いに到達可能な頂点が含まれる。 ($scc ~ v \subseteq V$)
    - $outs^\ast ~ v \neq \varnothing, ~ ins^\ast ~ v \neq \varnothing$ より $scc ~ v \neq \varnothing$

## 1. 「同じ強連結成分内」という関係は同値関係になる。

頂点集合 $V$ 上で次の関係 $\sim_{scc}$ を定義する。

$$
a \sim_{scc} b := b \in scc ~ a ~ (= outs^\ast ~ a \cap ins^\ast ~ a)
$$

この $\sim_{scc}$ は **同値関係** になることを示すには、反射律・対称律・推移律が成り立つことを示せばよい。

- **反射律**：  
  $a \in outs^\ast ~ a, ~ a \in ins^\ast ~ a$（0 本の辺で到達）より、反射律 $a \sim_{scc} a$ が成立する。

- **対称律**：  
  $a \sim_{scc} b$ が成り立つとすると、$(b \in outs^\ast ~ a) \land (b \in ins^\ast ~ a)$ となる。  
  すると、$b \in outs^\ast ~ a$ から $a \in ins^\ast ~ b$ となる。  
  また、$b \in ins^\ast ~ a$ から $a \in outs^\ast ~ b$となる。  
  したがって、$b \sim_{scc} a$ が成り立ち、対称律が成立する。

- **推移律**：  
  $a \sim_{scc} b$ と $b \sim_{scc} c$ が成立するなら、$b \in scc ~ a ~ (= outs^\ast ~ a \cap ins^\ast ~ a)$ より、$b \in outs^\ast ~ a$ であり、$outs^\ast$ の定義から、頂点 $a$ から0本以上の辺をたどって頂点 $b$ に到達可能である。  
  同様に、$c \in scc ~ b ~ (= outs^\ast ~ b \cap ins^\ast ~ b)$ より、$c \in outs^\ast ~ b$ であり、頂点 $b$ から0本以上の辺をたどって頂点 $c$ に到達可能である。  
  したがって、頂点 $b$ を経由することで頂点 $a$ から $c$ に到達可能であるから、$c \in outs^\ast ~ a$ となる。  
  $ins^\ast$ に関しても同様のことが成り立ち、$c \in ins^\ast ~ a$ となる。  
  したがって $a \sim_{scc} c$ となり、推移律が成立する。

よって、「同じ強連結成分内」という関係は **同値関係** である。

## 2. 「同じ強連結成分内」という同値関係による同値類は頂点集合を重複なく分割する

「同じ強連結成分内」という同値関係 $\sim_{scc}$ の同値類を以下の様に定義する。

$$
[a]_{\sim_{scc}} := \{ b \in V \mid a \sim_{scc} b \}
$$

同値類 $[v]_{\sim_{scc}} \quad (v \in V)$ は $V$ の部分集合であって次の性質をもつ。

* $v \in [v]_{\sim_{scc}}$
  - $\sim$ の反射律より明らか。

* $a \sim_{scc} b \implies [a]_{\sim_{scc}} = [b]_{\sim_{scc}}$
  - $c \in [a]_{\sim_{scc}}$ とすると, 同値類の定義から
$a \sim_{scc} c$ である。  
    これと仮定 $a \sim_{scc} b$ に対称律を適用した $b \sim_{scc} a$ を合わせて推移律を用いると $b \sim_{scc} c$ とな
る。  
    したがって, $c \in [b]_{\sim_{scc}}$ である。  
    こうして, $[a]_{\sim_{scc}} \subset [b]_{\sim_{scc}}$ が示された。  
    $a, b$ の役割を入れ替えて, 同様の議論を繰り返せば $[b]_{\sim_{scc}} \subset [a]_{\sim_{scc}}$ も示されるので $[a]_{\sim_{scc}} = [b]_{\sim_{scc}}$ である。

* $a, b \in V$ に対して, $[a]_{\sim_{scc}} = [b]_{\sim_{scc}}$ または $[a]_{\sim_{scc}} \cap [b]_{\sim_{scc}} = \varnothing$ のいずれか一
方だけが成り立つ.
  - 同値類は空集合ではないので
    $$[a]_{\sim_{scc}} = [b]_{\sim_{scc}}$$
    と
    $$[a]_{\sim_{scc}} \cap [b]_{\sim_{scc}} = \varnothing$$
    が同時に成り立つことはない。  
    したがって, 主張のためには,
    $$[a]_{\sim_{scc}} \cap [b]_{\sim_{scc}} \neq \varnothing \implies [a]_{\sim_{scc}} = [b]_{\sim_{scc}}$$
    が成り立てばよい。  
    仮定から  
    $$[a]_{\sim_{scc}} \cap [b]_{\sim_{scc}} \neq \varnothing$$
    なので $c \in [a]_{\sim_{scc}} \cap [b]_{\sim_{scc}}$ を満たす元 $c$ をとることができる。  
    同値類の定義から $a \sim_{scc} c$ かつ $b \sim_{scc} c$ である。  
    対称律と推移律によって $a \sim_{scc} b$ となる.  
    $a \sim_{scc} b \implies [a]_{\sim_{scc}} = [b]_{\sim_{scc}}$ によって $[a]_{\sim_{scc}} = [b]_{\sim_{scc}}$ が得られる。

よって、「同じ強連結成分内」という同値関係 $\sim_{scc}$ の同値類は、頂点集合を重複なく分割する。
